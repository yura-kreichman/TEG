DATABASE_URL="postgresql://teg:teg@localhost:5432/teg_saas?schema=public"

# Session/JWT secret for auth (login/password + PIN). Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-random-secret"

-- Сколько владелец забрал из зоны инкассацией ДО пересчёта (жалоба владельца
-- КидсБурга 2026-09-02: «разницы не реальные»). Деньги вышли из ящика мимо
-- сотрудника, поэтому для сверки со счётчиками их надо вернуть в выручку —
-- ровно так же, как уже возвращаются расходы. Nullable без default: NULL —
-- сдача старше этой миграции, там числа не существует, прошлое не меняем.
ALTER TABLE "ZoneSubmission"
  ADD COLUMN "collectedBeforeSubmission" DECIMAL(10,2);

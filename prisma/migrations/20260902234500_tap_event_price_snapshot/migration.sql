-- Цена тарифа в момент тапа (генеральная проверка финансов 2026-09-02).
-- Удаление тапа, оплаченного балансом целиком, возвращало клиенту ТЕКУЩУЮ
-- цену тарифа, а не списанную. Nullable без default: NULL — тап старше этой
-- миграции, для него сохраняется прежнее чтение текущей цены.
ALTER TABLE "CounterTapEvent"
  ADD COLUMN "priceSnapshot" DECIMAL(10,2);

-- Подвал квитанции возвращается (запрос пользователя 2026-08-12) — колонка с
-- тем же именем была удалена 2026-07-21 миграцией
-- 20260721230000_remove_receipt_footer из-за поломки печати на Bluetooth
-- ESC/POS-мосту; настоящая причина тех поломок нашлась 2026-07-25 и к подвалу
-- отношения не имела (преждевременный afterprint на Android). Подробности — в
-- комментарии у поля в schema.prisma.
--
-- Формат снова richtext (ProseMirror JSON), как было до 20260721150000: тот же
-- редактор, что в Инструктажах и Лендинге.
ALTER TABLE "Tenant" ADD COLUMN "receiptFooterContent" JSONB;

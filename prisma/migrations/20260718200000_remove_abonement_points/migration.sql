-- Абонемент (тариф-план) больше не привязан к точкам продажи (запрос
-- пользователя 2026-07-18: план всегда виден и продаваем на всех точках
-- тенанта, точка нужна только в момент самой оплаты — см.
-- AbonementTransaction.pointId). Implicit m2m таблица больше не нужна.
DROP TABLE "_AbonementPoints";

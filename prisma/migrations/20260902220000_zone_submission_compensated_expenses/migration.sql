-- Сколько расходов сдача вернула обратно в выручку (генеральная проверка
-- финансов 2026-09-02, находка С4). Nullable без default: NULL означает
-- «сдача сделана до этой миграции, числа не существует», и читающая сторона
-- для таких строк оставляет прежний расчёт по привязке расходов. Ноль —
-- законное значение новой сдачи, а не отсутствие данных.
ALTER TABLE "ZoneSubmission"
  ADD COLUMN "compensatedExpenses" DECIMAL(10,2);

-- Доля пула внутри суммы инкассации зоны (генеральная проверка финансов,
-- находка С51). Уведомление показывает ВВЕДЁННУЮ сумму, а пересборка
-- сообщения после правки восстанавливала её из журнала вместе с долей и
-- снова печатала «955 вместо 700». Nullable: у операций старше миграции и у
-- всех типов, кроме зонной инкассации, доли не бывает.
ALTER TABLE "MoneyOperation"
  ADD COLUMN "poolShareAmount" DECIMAL(10,2);
